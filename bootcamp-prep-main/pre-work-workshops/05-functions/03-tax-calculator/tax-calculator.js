// YOUR CODE BELOW
// function taxCalculator(amt,state){
//          if (state === 'NY'){
//             return amt * 1.04;
//         } else (state === 'NJ') {
//             return amt * 1.06625;
//         }
//     }
function taxCalculator(amt,state){
    if (state === 'NY'){
        return amt * 1.04;
    }
    else (state === 'NJ'); {
        return amt * 1.06625;
    }
}